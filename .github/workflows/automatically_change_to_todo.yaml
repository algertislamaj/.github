name: Assign issues

on:
  issues:
    types: [assigned]

jobs:
  createCard:
    runs-on: ubuntu-latest
    steps:
      - name: Create or Update Project Card
        uses: peter-evans/create-or-update-project-card@v3
        with:
          repository: algertislamaj/test2
          project-name: Test
          column-name: Todo
